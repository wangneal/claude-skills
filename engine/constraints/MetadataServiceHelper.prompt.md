# SDK 使用约束 - MetadataServiceHelper

> 此约束文件由 bos-flow 自动生成
> 生成时间: 2026-04-29T17:28:28.806Z

## 强制约束

在生成代码时，必须遵守以下约束：

### 1. 类名约束
- **必须使用**: `MetadataServiceHelper`
- **模块**: 核心框架
- **方法总数**: 2 个

- **完整包路径**: `kd.bos.servicehelper`

### 2. 方法约束

#### 允许使用的方法列表：

##### copyMobleFormMetadata
```java
java.util.Map<java.lang.String,java.lang.Object> copyMobleFormMetadata(java.lang.String, java.lang.String, java.lang.String, java.lang.String, LocaleString)
```
- **返回类型**: void

##### getDataEntityType
```java
MainEntityType getDataEntityType(java.lang.String)
```
- **返回类型**: MainEntityType

### 3. 禁止事项

❌ **禁止**使用以下不存在的方法：
- 请参考上述方法列表，不要猜测或臆造方法名

❌ **禁止**使用错误的参数类型：
- 参数类型必须与方法签名完全匹配
- 例如：如果签名是 `Long billId`，则不能使用 `String billId`

### 4. 最佳实践

✅ **推荐**在使用前检查条件
✅ **推荐**使用 try-catch 处理异常
✅ **推荐**参考官方文档和示例代码

---

*Generated by bos-flow kd-gen command*
